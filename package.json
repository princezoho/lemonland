{
  "name": "landing-page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://princezoho.github.io/lemonland/",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "three": "^0.176.0"
  }
}
